# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres 
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the 
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

APP_URL=http://localhost:3300

DATABASE_URL="postgresql://postgres:password@localhost:5434/ptm_bmup_finance?schema=public"
DATABASE_URL_DOCKER="postgresql://postgres:password@ptm_bmup_finance_db:5432/ptm_bmup_finance?schema=public"

# ENV
NODE_ENV=development
PORT=3300

# API URL CORE
API_URL_CORE=http://localhost:3000/api
API_URL_CORE_DOCKER=http://project_be_app_management:3000/api

# API URL SETTING
API_URL_SETTING=http://localhost:3200/api
API_URL_SETTING_DOCKER=http://ptm_bmup_setting:3200/api

# URL FE
APP_FRONTEND_URL=http://localhost:3100

POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=ptm_bmup_finance

RATE_LIMIT_MAX_REQUESTS=60
TZ=Asia/Jakarta

# APM Configuration
APM_API_KEY_FINANCE=
APM_SERVER_URL=http://localhost:8200
APM_SERVER_URL_DOCKER=http://apm-server:8200

# Business Config